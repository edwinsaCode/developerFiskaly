# Frontend env — digunakan saat run Next.js di luar Docker (npm run dev).
# Harus menunjuk port host tempat backend di-publish: samakan dengan
# BACKEND_HOST_PORT di .env root. Di dalam Docker nilai ini di-override
# compose menjadi http://backend:8080.
NEXT_PUBLIC_API_URL=http://localhost:8080
